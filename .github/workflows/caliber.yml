name: Caliber Score

on:
  pull_request:

jobs:
  caliber-score:
    name: AI Config Quality Score
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Caliber Score
        uses: caliber-ai-org/ai-setup@v1
        with:
          agent: 'cursor'
          comment: 'true'
          fail-below: '60'
        env:
          CALIBER_TELEMETRY_DISABLED: '1'
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
